import Lead from '../models/Lead.js';

export const extractEmails = async (req, res) => {
  // Placeholder – will implement after server runs
  res.json({ message: 'Email extractor coming soon' });
};

export const bulkExtractEmails = async (req, res) => {
  res.json({ message: 'Bulk extract coming soon' });
};

export const saveExtractedLeads = async (req, res) => {
  res.json({ message: 'Save leads coming soon' });
};

export const bulkSendEmail = async (req, res) => {
  res.json({ message: 'Bulk email coming soon' });
};
